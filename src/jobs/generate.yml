version: 2.1

orbs:
  sbom: manifest-cyber/sbom@<< pipeline.git.revision >>
description: >
  Sample description

executor: default

parameters:
  source:
    type: string
    default: "."
    description: "source directory to generate sbom for"
steps:
  - checkout
  - sbom/install:
      version: "latest"
      