description: >
  Sample description
# What will this job do?

executor: default

parameters:
  source:
    type: string
    default: "."
    description: "source directory to generate sbom for"
steps:
  - checkout
  - sbom/install:
      version: "latest"